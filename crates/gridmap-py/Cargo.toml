[package]
name = "gridmap-py"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
name = "_core"
crate-type = ["cdylib"]

[dependencies]
gridmap-core = { path = "../gridmap-core" }
pyo3 = { version = "0.22", features = ["extension-module"] }
